tmpl f(e: A, m: Number = 5): C {
    (c: C)[?]

    foreach i in @range(0, m):
        if i > 2: 
            (n: N)[i]
            c -->% n
        /if
    /foreach

    e -->* c
} => c

(a: A)[?]
f(e -> a) => c