tmpl accumulator(e: A, n: Number = 1): A {
    (e2: A)[e.value + n]
} => e2

(a: A)[0]

accumulator(e -> a) => a2
accumulator(e -> a2) => a3
accumulator(e -> a3) => a4
accumulator(e -> a4) => a5
accumulator(e -> a5) => a6
accumulator(e -> a6) => a7
accumulator(e -> a7) => a8
accumulator(e -> a8) => a9
accumulator(e -> a9) => a10
