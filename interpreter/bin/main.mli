val start : unit
(** [start ()] initializes the interpreter. It sets up the necessary environment
    and prepares for execution. This function should be called before any other
    operations are performed. *)
