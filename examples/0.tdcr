# A simple example of a TDCR file
# This should give a type error because of the duplicate id `a`

tmpl f(n: Number) A {
    (a: A)[n]
} => a

(a: A)[0]
%(b: B)[?: Number]

f(n = 1) => a

a -->+ b
b -->% a
